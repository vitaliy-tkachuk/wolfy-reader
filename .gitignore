# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Env
.env
.env.local
.env.*.local

# Ephemeral local task files (durable knowledge lives in docs/domains/ + docs/architecture.md)
docs/tasks/

# Graphify: the graph itself is committed so every clone gets cheap context.
# The AST cache is local scratch — regenerated on demand, and pure churn in diffs.
graphify-out/cache/

# Logs
*.log

# Stack-specific dependency/build ignores are appended below by /describe-project
# based on the chosen project type. Until then the base above stays lean.
