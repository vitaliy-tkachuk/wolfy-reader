# Architecture

> This file holds global architecture **and** cross-cutting decisions (anything that shapes the project as a whole — stack/runtime, persistence, identity, distribution, etc.) woven into the section each affects — date significant ones inline. Domain-local decisions live in `docs/domains/<domain>.md` instead. There is no separate decisions folder.

## Purpose

Short description of what this application does.

> TBD — fill in once the project has a clear product purpose.

## Current stack

> Filled by `/describe-project`. One bullet per significant tech choice (language, runtime, framework, data layer, identity, distribution, etc.) — only the categories that actually apply to this project. Date significant choices inline.

- TBD — stack not yet established.

## Main application areas

- TBD — source tree not yet established.

## Important boundaries

Document architectural rules here as they are decided. Examples vary by stack and project shape — record only what applies here.

## Known constraints

- None recorded yet.
