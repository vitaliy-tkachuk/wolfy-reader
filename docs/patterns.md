# Patterns

Cross-domain reusable patterns in this repository. Add after the 3rd use, not the 1st. 1st = solution, 2nd = coincidence, 3rd = pattern.

Patterns that only matter inside a single domain go in that domain's `## Patterns` section in `docs/domains/<domain>.md`, not here. This file is for patterns that span multiple domains.

## Pattern template

### Pattern name

Use when:

- ...

Implementation:

- ...

Example files:

- `src/...`

## Anti-pattern template

Avoid:

- ...

Why:

- ...

Do instead:

- ...
